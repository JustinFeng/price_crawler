<h2>New item</h2>

<%= render "form.html", changeset: @changeset, products: @products,
                        action: order_item_path(@conn, :create, @order) %>

<%= link "Back", to: order_item_path(@conn, :index, @order) %>
