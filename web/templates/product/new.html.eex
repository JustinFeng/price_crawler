<h2>New product</h2>

<%= render "form.html", changeset: @changeset, vendors: @vendors,
                        action: product_path(@conn, :create) %>

<%= link "Back", to: product_path(@conn, :index) %>
